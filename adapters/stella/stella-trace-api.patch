diff --git a/src/emucore/M6502.hxx b/src/emucore/M6502.hxx
index d5b8ccf..4cd16a4 100644
--- a/src/emucore/M6502.hxx
+++ b/src/emucore/M6502.hxx
@@ -61,6 +61,15 @@ class M6502 : public Serializable
 
     using onHaltCallback = std::function<void()>;
 
+    // gbtrace adapter: read-only accessors for the register file (the
+    // registers are otherwise private, exposed only to the debugger).
+    uInt16 gbPC() const { return PC; }
+    uInt8  gbA()  const { return A;  }
+    uInt8  gbX()  const { return X;  }
+    uInt8  gbY()  const { return Y;  }
+    uInt8  gbSP() const { return SP; }
+    uInt8  gbPS() const { return PS(); }
+
   public:
     /**
       Create a new 6502 microprocessor.
diff --git a/src/emucore/tia/TIA.hxx b/src/emucore/tia/TIA.hxx
index 124d729..1277840 100644
--- a/src/emucore/tia/TIA.hxx
+++ b/src/emucore/tia/TIA.hxx
@@ -278,6 +278,12 @@ class TIA : public Device
     Int32 maxVcenter() const { return myFrameManager->maxVcenter(); }
     uInt32 startLine() const { return myFrameManager->startLine(); }
 
+    // gbtrace adapter: frame-manager state for VSYNC-anchoring the snapshot.
+    // The framebuffer row being rendered IS getY(); vsync() is the current
+    // VSYNC state — together they locate the VSYNC-deassert row.
+    bool gbVSync() const { return myFrameManager->vsync(); }
+    uInt32 gbY() const { return myFrameManager->getY(); }
+
     /**
       Changes the current vcenter property.
     */
